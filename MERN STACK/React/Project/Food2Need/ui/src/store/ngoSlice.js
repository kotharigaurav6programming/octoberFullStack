import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};
const ngoSlice = createSlice({
    name:"ngoSlice",
    initialState,
    reducers:{

    }
});

//export {} = ngoSlice.actions;
export default ngoSlice.reducer;