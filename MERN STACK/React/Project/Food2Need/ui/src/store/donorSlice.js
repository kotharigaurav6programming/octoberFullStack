import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};
const donorSlice = createSlice({
    name:"donorSlice",
    initialState,
    reducers:{

    }
});

//export {} = donorSlice.actions;
export default donorSlice.reducer;